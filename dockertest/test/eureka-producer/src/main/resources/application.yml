
info:
  description: ${test}

server:
  port: 1111

eureka:
  instance:
    hostname: producer
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/

endpoints:
  restart:
    enabled: true
