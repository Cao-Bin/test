spring:
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
  application:
    name: bus
  cloud:
    config:
      uri: http://config:8888
#      name: bus
#      profile: ${config.profile:dev}
#      label: master
